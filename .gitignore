# -------------------------------------------------------------------
# General Exclusion for Web Projects
# -------------------------------------------------------------------

# Compiled files and caches (often generated by build tools or IDEs)
*.log
*.tmp
.DS_Store
Thumbs.db
/dist/
/build/

# Node.js dependencies (if you use npm/yarn for any reason)
/node_modules

# -------------------------------------------------------------------
# Environment Specific (Good practice for local development/testing)
# -------------------------------------------------------------------

# Temporary or sensitive config files
.env
.env.local

# Image folder (if you use a separate folder for original/large assets
# and only commit optimized images, or if you generate thumbnails)
# Since you have image tags like 'images/fam icon.webp', you are
# committing your image directory, so we will not exclude it.
# To exclude an images directory, you would use:
# /images/